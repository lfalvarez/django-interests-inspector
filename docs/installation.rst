============
Installation
============

At the command line::

    $ easy_install django-interests-inspector

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-interests-inspector
    $ pip install django-interests-inspector
